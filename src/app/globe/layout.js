// import "../globals.css";

export const metadata = {
  title: "WebGL globe",
  description: "WebGL globe",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
